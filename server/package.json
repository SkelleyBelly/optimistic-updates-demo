{
  "name": "server",
  "version": "1.0.0",
  "description": "Server the provides a GraphQL and Rest endpoint for the optimistic UI example project",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "nodemon"
  },
  "dependencies": {
    "apollo-server-express": "^2.20.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "ts-node": "^9.1.1",
    "@types/express": "^4.17.11",
    "nodemon": "^2.0.7"
  },
  "nodemonConfig": {
    "execMap": {
      "ts": "node -r ts-node/register"
    },
    "ext": "js,json,ts"
  }
}
